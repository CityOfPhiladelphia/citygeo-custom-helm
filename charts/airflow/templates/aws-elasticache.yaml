---
apiVersion: elasticache.services.k8s.aws/v1alpha1
kind: ReplicationGroup
metadata:
  name: {{ .Release.Name }}-valkey
  # annotations:
  #   services.k8s.aws/adoption-policy: "adopt-or-create"
  #   services.k8s.aws/adoption-fields: |
  #     {"replicationGroupID": "{{ .name }}-{{ .Values.envName }}"}
spec:
  replicationGroupID: "{{ .Release.Name }}-{{ .Values.envName }}"
  description: Valkey Cluster for Airflow Celery Workers
  cacheNodeType: cache.t4g.micro
  kmsKeyID: "{{ .Values.aws.kmsKeyId }}"
  engine: valkey
  engineVersion: "9.0"
  securityGroupIDs:
    - "{{ .Values.aws.elasticacheSgId }}"
  cacheSubnetGroupName: "{{ .Values.aws.elasticacheSubnetGroup }}"
  transitEncryptionEnabled: true
  atRestEncryptionEnabled: true
  authToken:
    name: airflow-valkey-creds
    key: password
---
apiVersion: services.k8s.aws/v1alpha1
kind: FieldExport
metadata:
  name: {{ .Release.Name }}-valkey
spec:
  from:
    path: ".status.nodeGroups[0].primaryEndpoint"
    resource:
      group: rds.services.k8s.aws
      kind: DBInstance
      name: openmetadata-postgres
  to:
    name: ack-outputs
    kind: secret
